package com.tickethub.domain;

/**
 * Marker for value objects: immutable, equality by attributes.
 * An interface (not a class) so Java records can implement it.
 */
public interface ValueObject {
}
